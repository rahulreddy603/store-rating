# Server
server.port=8080

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/store_rating_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Rahul@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
#noinspection UnknownProperty
app.jwt.secret=RahulStoreRatingProjectSecretKey2026SpringBootJWT
#noinspection UnknownProperty
app.jwt.expiration-ms=86400000

# Custom CORS Configuration (Renamed to avoid IDE conflicts)
app.cors.allowed-origins=http://localhost:3000
# Logging
logging.level.com.storerating=INFO
logging.level.org.springframework.security=WARN